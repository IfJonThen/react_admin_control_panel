import Rebase from 're-base';
/*eslint no-unused-vars: "off"*/

// import * as firebase from 'firebase';

export const base = Rebase.createClass({
    
});
// export base.database().ref();
// export const firebaseAuth = base.auth();

var config = {
    
};
